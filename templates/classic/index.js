/**
 * TEMPLATE 1 - "CLASSIC"
 * The original design, untouched. It renders the existing
 * component tree and is styled by styles/portfolio.css.
 */
import Layout from '../../components/layout/Layout';

export default function ClassicTemplate() {
  return <Layout />;
}
